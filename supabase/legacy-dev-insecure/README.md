# Legacy Dev SQL - Do Not Run In Production

These files are kept only as historical setup references.

They contain permissive development policies such as public read/write access.
Do not run these files against production or any internet-facing Supabase
project.

Use the timestamped files in `supabase/migrations/` instead.
